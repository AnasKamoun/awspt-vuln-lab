# FAKE SSH private key — real key material intentionally stripped so the lab
# stays pushable. The FILE NAME alone (id_rsa) is the finding: a private key
# committed to version control. awspt flags it via its committed-key-file pass.
# (For PEM-content detection see vulns/crypto_secrets.py, which carries a full
#  -----BEGIN RSA PRIVATE KEY----- block.)
