FAKE PKCS#12 keystore placeholder — not real DER bytes. awspt flags any
committed .p12/.pfx/.jks/.keystore by type (a binary keystore can't be
content-scanned, so its presence in the repo is itself the finding).
