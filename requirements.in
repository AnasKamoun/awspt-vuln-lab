# Extra (NOT installed by the Dockerfile) — deliberately ancient, CVE-laden
# pins so the SCA pass has plenty to flag via OSV. Do not `pip install` this.
Django==2.2.0
Flask==0.12.2
Werkzeug==0.14.1
urllib3==1.24.1
PyYAML==5.1
lxml==4.6.2
cryptography==2.3
paramiko==2.4.1
requests==2.19.1
Pillow==5.0.0
oauthlib==2.0.0
Jinja2==2.10
