# Runtime deps for the vulnerable app. Pinned to OLD-but-installable versions
# that carry known CVEs (so SCA flags them) while still running on py3.12.
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.6
jinja2==3.1.2
requests==2.31.0
pyyaml==6.0.1
